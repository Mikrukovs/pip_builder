# 🧪 Testing Guide

## Что было реализовано

### ✅ Этап 1: Backend Infrastructure
- PostgreSQL база данных
- Prisma ORM
- 5 таблиц (users, folders, projects, collaborators, history)

### ✅ Этап 2: REST API
- Authentication (JWT)
- Folders CRUD
- Projects CRUD
- Collaborators Management
- Система ролей (owner, editor, viewer)

### ✅ Этап 3: Dashboard UI
- Страница авторизации
- Dashboard со списками
- Модальные окна
- Автосохранение в БД

## 🚀 Запуск для тестирования

### 1. Обновите код

```bash
cd ~/Ioflan4ik  # или где у вас проект
git pull origin dev
```

### 2. Настройте .env

Убедитесь что в `.env` есть:

```env
# Database
DATABASE_URL="postgresql://postgres:postgres@postgres:5432/prototype_builder"
POSTGRES_DB=prototype_builder
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# NextAuth
NEXTAUTH_SECRET="your-secret-min-32-characters-long"
NEXTAUTH_URL="http://localhost:8888"

# Telegram Bot (настройте через @BotFather)
TELEGRAM_BOT_TOKEN=your_token
NEXT_PUBLIC_TELEGRAM_BOT_NAME=your_bot_name
```

**Генерация NEXTAUTH_SECRET:**
```bash
openssl rand -base64 32
```

### 3. Запустите Docker

```bash
# Остановите старые контейнеры
docker compose down -v

# Запустите новые
docker compose up -d

# Проверьте статус
docker compose ps
```

### 4. Примените миграции

```bash
# Применить миграции к БД
docker compose exec app-dev npx prisma migrate deploy

# Проверить что таблицы созданы
docker compose exec app-dev npx prisma studio
```

Prisma Studio откроется на `http://localhost:5555`

### 5. Откройте приложение

Откройте `http://localhost:8888`

## ✅ Тест-кейсы

### Test 1: Авторизация

1. Откройте `http://localhost:8888`
2. Должен открыться `/login`
3. Нажмите "Log in with Telegram"
4. Авторизуйтесь через Telegram
5. ✅ Должен открыться Dashboard

### Test 2: Создание папки

1. На Dashboard нажмите "+ Создать папку"
2. Введите название "Мои прототипы"
3. Нажмите "Создать"
4. ✅ Папка появится в списке

### Test 3: Создание проекта

1. Нажмите "+ Создать проект"
2. Введите название "Тестовый проект"
3. Выберите папку (или оставьте "Без папки")
4. Нажмите "Создать"
5. ✅ Откроется редактор проекта

### Test 4: Редактирование проекта

1. В редакторе добавьте компоненты
2. Подождите 3 секунды
3. ✅ Должна появиться галочка "Сохранено"
4. Обновите страницу (F5)
5. ✅ Изменения должны сохраниться

### Test 5: Просмотр проектов в папке

1. Вернитесь на Dashboard (клик на лого)
2. Кликните на созданную папку
3. ✅ Должны отобразиться проекты в папке

### Test 6: Выход и повторный вход

1. Нажмите "Выйти" в профиле
2. ✅ Должен открыться `/login`
3. Снова войдите через Telegram
4. ✅ Должны увидеть свои папки и проекты

## 🐛 Troubleshooting

### Ошибка: Cannot connect to database

```bash
# Проверьте что PostgreSQL запущен
docker compose ps

# Перезапустите
docker compose restart postgres

# Проверьте логи
docker compose logs postgres
```

### Ошибка: Prisma Client not generated

```bash
docker compose exec app-dev npx prisma generate
docker compose restart app-dev
```

### Ошибка: Tables don't exist

```bash
# Применить миграции
docker compose exec app-dev npx prisma migrate deploy

# Или пересоздать БД
docker compose down -v
docker compose up -d
docker compose exec app-dev npx prisma migrate deploy
```

### Telegram авторизация не работает

1. Проверьте `.env`:
   - `TELEGRAM_BOT_TOKEN` корректен
   - `NEXT_PUBLIC_TELEGRAM_BOT_NAME` без @

2. Проверьте домен в @BotFather:
   - Команда: `/setdomain`
   - Укажите: `localhost`

3. Проверьте логи:
```bash
docker compose logs -f app-dev
```

## 📊 Проверка БД

### Через Prisma Studio

```bash
docker compose exec app-dev npx prisma studio
```

Откройте `http://localhost:5555` и проверьте:
- Таблица `users` - ваш пользователь создан
- Таблица `folders` - созданные папки
- Таблица `projects` - созданные проекты
- Таблица `project_collaborators` - вы owner своих проектов

### Через psql

```bash
# Подключитесь к PostgreSQL
docker compose exec postgres psql -U postgres -d prototype_builder

# Проверьте таблицы
\dt

# Проверьте пользователей
SELECT * FROM users;

# Проверьте проекты
SELECT id, name, folder_id FROM projects;

# Выйти
\q
```

## 🎉 Что работает

- ✅ Полная авторизация через Telegram
- ✅ Создание/редактирование/удаление папок
- ✅ Создание/редактирование проектов
- ✅ Автосохранение в БД
- ✅ Система прав доступа
- ✅ Навигация между страницами

## ⏰ Что осталось (опционально)

- ⏳ WebSocket для real-time collaboration
- ⏳ Синхронизация между пользователями
- ⏳ Live cursors
- ⏳ Conflict resolution

## 📝 Известные ограничения

1. **Автосохранение**: происходит каждые 3 секунды, может быть задержка
2. **Concurrent editing**: пока не реализовано (WebSocket)
3. **История изменений**: записывается, но UI для просмотра пока нет

## 🚀 Следующие шаги

1. **Протестируйте** все базовые функции
2. **Проверьте** что данные сохраняются в БД
3. **Скажите** если что-то не работает
4. **Решите** нужен ли WebSocket для collaborative editing

Удачи! 🎊
